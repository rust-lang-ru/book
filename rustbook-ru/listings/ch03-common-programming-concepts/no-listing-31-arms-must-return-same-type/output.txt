$ cargo run
   Сборка branches v0.1.0 (file:///projects/branches)
ошибка[E0308]: `if` and `else` have incompatible types
 --> src/main.rs:4:44
  |
4 |     let число = if условие { 5 } else { "six" };
  |                                 -          ^^^^^ expected integer, found `&str`
  |                                 |
  |                                 expected because of this

Для получения более подробных сведений об этой ошибке, попробуйте выполнить `rustc --explain E0308`.
ошибка: не могу собрать исходную рукопись в приложение `branches` (bin "branches") due to 1 предыдущая ошибка
