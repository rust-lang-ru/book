$ cargo run
   Сборка cons-список v0.1.0 (file:///projects/cons-список)
ошибка[E0382]: use of moved значение: `a`
  --> src/main.rs:11:30
   |
9  |     let a = Cons(5, Box::new(Cons(10, Box::new(Nil))));
   |         - move occurs because `a` has type `Список`, which does not implement the `Copy` trait
10 |     let b = Cons(3, Box::new(a));
   |                              - значение moved here
11 |     let c = Cons(4, Box::new(a));
   |                              ^ значение used here after move

Для получения более подробных сведений об этой ошибке, попробуйте запустить `rustc --explain E0382`.
ошибка: не могу собрать исходную рукопись в приложение `cons-список` (bin "cons-список") из-за 1 предыдущей ошибки
